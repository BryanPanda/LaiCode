package restoreFromCountArray;

// There is an original unsorted array A of size n that contains all integers from
// [1, ..., n] (no duplicated numbers), but we do not know A, all we know is a count-array B, 
// where B[i] represents the number of elements A[j]  (i < j) that are smaller than A[i]. 
// How can we re-store A based on B?

// Assumption: The given array is not null.

public class RestoreFromCountArray {

	public int[] restore(int[] array) {
		return array;
	}

}
